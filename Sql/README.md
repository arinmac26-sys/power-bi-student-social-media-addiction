# SQL

The current project uses Excel as the source and Power BI as the analytical layer, so no SQL source script is required.

If this project is migrated to a database, recommended tables are:

- `dim_student`
- `dim_platform`
- `dim_date`
- `fact_student_social_media`

Add the SQL DDL and data-load scripts here when the database version is created.
